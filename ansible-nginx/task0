while true; do echo "$(curl -s -o /dev/null -w "%{http_code} %{time_total}" https://s3.mdst.yandex.net/ping) $(date +%s) "; sleep 10; done
while true; do sleep 10;  echo "$(curl -s -o /dev/null -w "%{http_code} %{time_total}" ya.ru) $(date +%s) "; done